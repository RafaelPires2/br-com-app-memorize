import { HBox } from '@atomic/obj.grid';
import styled from 'styled-components/native';

export const CheckBoxWrapper = styled(HBox)`
  width: 50%;
  justify-content: center;
  align-items: center;
`;
